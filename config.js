// ─────────────────────────────────────────────────────────────────
// Change this to your local IP when testing on a physical device.
// DO NOT use localhost — your phone can't reach your computer via localhost.
//
// Find your local IP:
//   macOS / Linux : run `ifconfig` → look for en0 inet (e.g. 192.168.1.10)
//   Windows       : run `ipconfig` → look for IPv4 Address
//
//
// ─────────────────────────────────────────────────────────────────

export const API_URL = "http://172.32.93.98:5000/api";

// ─── App Color Palette ────────────────────────────────────────────────────────
export const COLORS = {
  blue: "#1736F5",
  yellow: "#FFC709",
  gray: "#808080",
  lightGray: "#DEDEDE",
  white: "#FFFFFF",
  black: "#1a1a1a",
  danger: "#E53935",
};
